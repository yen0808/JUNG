# CLAUDE.md

This file provides guidance to AI assistants (Claude and others) working in this repository.

---

## Repository Status

This repository is currently empty / in initial setup. This file should be updated as the project evolves.

---

## Project Overview

> **TODO:** Add a brief description of what this project does, its purpose, and its intended users.

---

## Technology Stack

> **TODO:** Document the languages, frameworks, and major libraries used once the project is initialized.

---

## Repository Structure

> **TODO:** Document the directory layout once the project has files. Example format:

```
/
├── src/           # Application source code
├── tests/         # Test files
├── docs/          # Documentation
└── ...
```

---

## Development Setup

> **TODO:** Document how to set up a local development environment. Include:
> - Prerequisites (runtime versions, tools)
> - Installation steps
> - Environment variables required

---

## Common Commands

> **TODO:** Fill in the actual commands once the project is set up.

| Task            | Command              |
|-----------------|----------------------|
| Install deps    | `<command>`          |
| Start dev server| `<command>`          |
| Run tests       | `<command>`          |
| Lint            | `<command>`          |
| Format          | `<command>`          |
| Build           | `<command>`          |

---

## Code Conventions

> **TODO:** Document project-specific conventions. General defaults until overridden:

- Follow the style of existing code in any file being edited.
- Do not introduce new dependencies without discussion.
- Keep functions small and focused on a single responsibility.
- Prefer explicit over implicit.

---

## Testing

> **TODO:** Describe the testing strategy, frameworks used, and how to run tests.

- Always run the test suite before committing.
- New features should include corresponding tests.
- Bug fixes should include a regression test where practical.

---

## Git Workflow

- Branch naming: `feature/<description>`, `fix/<description>`, `chore/<description>`
- Write clear, descriptive commit messages in the imperative mood (e.g., "Add login page", not "Added login page").
- Keep commits focused — one logical change per commit.
- Do not commit secrets, credentials, or large binary files.

---

## AI Assistant Guidelines

- Read relevant files before making changes — do not guess at structure.
- Prefer editing existing files over creating new ones.
- Do not add speculative abstractions or features not explicitly requested.
- Do not modify files unrelated to the task at hand.
- Run linting/tests after making changes and fix any failures before considering the task done.
- Update this CLAUDE.md file whenever project structure or conventions change significantly.
